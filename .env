# 全站统一端口配置（单一事实来源）
# 本地 vite dev / vite preview 与 docker-compose 对外映射都读这里，
# 改一处即可保证两处口径一致。
PORT=8081

# 容器内 nginx 监听端口（仅容器内部使用，默认与对外端口一致，如无特殊需要不用改）
CONTAINER_PORT=8081

# 后端服务地址（nginx 容器内 /api 代理目标；本地 vite proxy 在 scripts/serve.mjs 中读取同一变量）
# 默认指向本机后端，生产环境通过环境变量覆盖，例如：
#   BACKEND_URL=http://backend:8080 docker compose up -d
BACKEND_URL=http://host.docker.internal:8080
